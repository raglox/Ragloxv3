# RAGLOX v3.0 Design System
## دليل التصميم الشامل للواجهة الحقيقية

---

## 1. نظرة عامة

هذه الوثيقة تحدد نظام التصميم المعتمد لمنصة RAGLOX v3.0 للعمليات الأمنية. التصميم مستوحى من واجهة Manus AI مع تحويرات تخدم السياق الأمني.

### 1.1 فلسفة التصميم

| المبدأ | الوصف |
|--------|-------|
| **الدفء** | ألوان رمادية دافئة محايدة (ليست زرقاء) |
| **النعومة** | زوايا مستديرة كبيرة، ظلال منتشرة |
| **التنفس** | مساحات بيضاء كافية، padding سخي |
| **الطبقات** | استخدام الظلال بدلاً من الحدود الصلبة |
| **الاحترافية** | مظهر enterprise-grade مثل Datadog/Splunk |

---

## 2. لوحة الألوان

### 2.1 الألوان الأساسية

| الاسم | الكود | الاستخدام |
|-------|-------|----------|
| `--background` | `#1a1a1a` | خلفية المحتوى الرئيسي |
| `--foreground` | `#e8e8e8` | النص الأساسي (أبيض دافئ) |
| `--card` | `#2a2a2a` | خلفية البطاقات |
| `--sidebar` | `#141414` | خلفية الشريط الجانبي |
| `--terminal` | `#0d0d0d` | خلفية الطرفية |
| `--muted-foreground` | `#888888` | النص الثانوي |
| `--border` | `rgba(255,255,255,0.06)` | الحدود (شبه غير مرئية) |

### 2.2 ألوان العلامات

| الاسم | الكود | الاستخدام |
|-------|-------|----------|
| `--primary` | `#4a9eff` | الأزرق الأساسي (الروابط، الأزرار) |
| `--success` | `#4ade80` | النجاح، الاكتمال |
| `--warning` | `#f59e0b` | التحذير، شارة Max |
| `--critical` | `#ef4444` | الخطر، CRITICAL |
| `--terminal-prompt` | `#4ade80` | لون الـ prompt في الطرفية |

### 2.3 الظلال

```css
--shadow-sm: 0 2px 8px rgba(0, 0, 0, 0.12);
--shadow-md: 0 4px 16px rgba(0, 0, 0, 0.18);
--shadow-lg: 0 4px 24px rgba(0, 0, 0, 0.22);
--shadow-card: 0 2px 8px rgba(0, 0, 0, 0.1), 0 4px 24px rgba(0, 0, 0, 0.15);
--shadow-card-hover: 0 4px 12px rgba(0, 0, 0, 0.15), 0 8px 32px rgba(0, 0, 0, 0.2);
```

---

## 3. Typography

### 3.1 الخطوط

| النوع | الخط | الحجم |
|-------|------|-------|
| **Body** | Inter | 14px |
| **Code** | JetBrains Mono | 13px |
| **Headings** | Inter | 16-24px |

### 3.2 أوزان الخط

- **Regular (400)**: النص العادي
- **Medium (500)**: العناوين، الأسماء
- **Semibold (600)**: الشارات، الأزرار المهمة

---

## 4. المسافات والأبعاد

### 4.1 نظام المسافات

| المتغير | القيمة | الاستخدام |
|---------|--------|----------|
| `--space-1` | 4px | مسافات صغيرة جداً |
| `--space-2` | 8px | مسافات صغيرة |
| `--space-3` | 12px | مسافات متوسطة |
| `--space-4` | 16px | padding البطاقات |
| `--space-6` | 24px | المسافة بين العناصر الرئيسية |

### 4.2 الزوايا المستديرة

| المتغير | القيمة | الاستخدام |
|---------|--------|----------|
| `--radius-sm` | 6px | الشارات الصغيرة |
| `--radius-md` | 8px | الأزرار |
| `--radius-lg` | 12px | البطاقات |
| `--radius-xl` | 16px | الـ overlays |
| `--radius-full` | 24px | صندوق الإدخال، pills |

---

## 5. المكونات الموجودة

### 5.1 مكونات Manus المخصصة

| المكون | الملف | الوصف |
|--------|-------|-------|
| `DualPanelLayout` | `DualPanelLayout.tsx` | التخطيط الرئيسي (Sidebar + Chat + Terminal) |
| `AIChatPanel` | `AIChatPanel.tsx` | لوحة الدردشة مع الأحداث |
| `TerminalPanel` | `TerminalPanel.tsx` | لوحة الطرفية |
| `Sidebar` | `Sidebar.tsx` | الشريط الجانبي (أيقونات فقط) |
| `ApprovalCard` | `ApprovalCard.tsx` | بطاقة طلب الموافقة HITL |
| `AIPlanCard` | `AIPlanCard.tsx` | بطاقة خطة AI |
| `CredentialCard` | `ArtifactCard.tsx` | بطاقة بيانات الاعتماد |
| `SessionCard` | `ArtifactCard.tsx` | بطاقة الجلسة |
| `VulnerabilityCard` | `ArtifactCard.tsx` | بطاقة الثغرة |
| `TargetCard` | `ArtifactCard.tsx` | بطاقة الهدف |
| `EventCard` | `EventCard.tsx` | بطاقة الحدث العامة |
| `PlanView` | `PlanView.tsx` | عرض الخطة |

### 5.2 مكونات UI الأساسية (shadcn/ui)

المكونات التالية متوفرة من مكتبة shadcn/ui ومُعدّلة لتتناسب مع التصميم:

| المكون | الاستخدام |
|--------|----------|
| `Button` | الأزرار |
| `Card` | البطاقات الأساسية |
| `Dialog` | النوافذ المنبثقة |
| `Input` | حقول الإدخال |
| `Textarea` | مناطق النص |
| `Badge` | الشارات |
| `ScrollArea` | مناطق التمرير |
| `Tabs` | علامات التبويب |
| `Table` | الجداول |
| `Progress` | شريط التقدم |
| `Tooltip` | التلميحات |
| `DropdownMenu` | القوائم المنسدلة |
| `Select` | قوائم الاختيار |
| `Checkbox` | مربعات الاختيار |
| `Switch` | المفاتيح |
| `Skeleton` | حالات التحميل |
| `Alert` | التنبيهات |

---

## 6. أنماط CSS المخصصة

### 6.1 شارة Agent (Max)

```css
.agent-badge {
  background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
  color: #000;
  font-size: 11px;
  font-weight: 600;
  padding: 2px 8px;
  border-radius: 6px;
}
```

### 6.2 شارة Knowledge

```css
.knowledge-badge {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 6px 12px;
  border-radius: 24px;
  font-size: 13px;
  background: transparent;
  color: #888888;
}
```

### 6.3 شارة Command

```css
.command-badge {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 4px 12px;
  border-radius: 6px;
  font-size: 13px;
  background: #2a2a2a;
  color: #888888;
}

.command-badge code {
  color: #e8e8e8;
  font-family: 'JetBrains Mono', monospace;
}
```

### 6.4 صندوق الإدخال

```css
.chat-input {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 8px 12px;
  border-radius: 24px;
  background: #2a2a2a;
  border: 1px solid rgba(255, 255, 255, 0.06);
}
```

### 6.5 بطاقة الحدث

```css
.event-card {
  border-radius: 12px;
  box-shadow: var(--shadow-card);
  background: #1f1f1f;
  /* NO border - rely on shadow */
}
```

---

## 7. الأيقونات

### 7.1 مكتبة الأيقونات

نستخدم **Lucide React** كمكتبة أيقونات أساسية.

### 7.2 الأيقونات المستخدمة

| الأيقونة | الاستخدام |
|----------|----------|
| `Shield` | شعار RAGLOX |
| `Brain` | Agent AI |
| `Terminal` | الطرفية، الأوامر |
| `Check` | الاكتمال، النجاح |
| `AlertTriangle` | التحذير |
| `AlertCircle` | الخطأ |
| `ChevronDown/Up` | التوسيع/الطي |
| `Plus` | إضافة |
| `Mic` | التسجيل الصوتي |
| `ArrowUp` | إرسال |
| `Copy` | نسخ |
| `Maximize2` | تكبير |
| `X` | إغلاق |
| `Key` | بيانات الاعتماد |
| `Network` | الجلسات |
| `Bug` | الثغرات |
| `Target` | الأهداف |
| `Lock` | الأمان |
| `Database` | قواعد البيانات |

### 7.3 حجم الأيقونات

| الحجم | الاستخدام |
|-------|----------|
| 16px | داخل الشارات والأزرار الصغيرة |
| 20px | أيقونات الشريط الجانبي |
| 24px | أيقونات رئيسية |

---

## 8. الانتقالات والحركة

### 8.1 مدة الانتقالات

| المتغير | القيمة | الاستخدام |
|---------|--------|----------|
| `--transition-fast` | 150ms | hover states |
| `--transition-normal` | 200ms | معظم الانتقالات |
| `--transition-slow` | 300ms | فتح/إغلاق panels |

### 8.2 Framer Motion

نستخدم Framer Motion للحركات المعقدة:

```tsx
// مثال: فتح الطرفية
<motion.div
  initial={{ width: 0, opacity: 0 }}
  animate={{ width: 500, opacity: 1 }}
  exit={{ width: 0, opacity: 0 }}
  transition={{ type: "spring", damping: 25, stiffness: 300 }}
/>
```

---

## 9. Types و Interfaces

### 9.1 أنواع البيانات الأساسية

```typescript
// Mission Status
enum MissionStatus {
  CREATED, STARTING, RUNNING, PAUSED,
  WAITING_FOR_APPROVAL, COMPLETING, COMPLETED,
  FAILED, CANCELLED, ARCHIVED
}

// Severity Levels
enum Severity {
  CRITICAL, HIGH, MEDIUM, LOW, INFO
}

// Session Types
enum SessionType {
  SHELL, METERPRETER, SSH, RDP, WMI, WINRM, SMB
}
```

### 9.2 واجهات المكونات

```typescript
// بطاقة الحدث
interface EventCard {
  id: string;
  type: WebSocketEventType;
  title: string;
  description?: string;
  timestamp: string;
  command?: string;
  knowledge?: KnowledgeItem[];
  approval?: ApprovalRequest;
  aiPlan?: AIPlanData;
  artifact?: ArtifactData;
}

// طلب الموافقة
interface ApprovalRequest {
  action_id: string;
  action_type: string;
  action_description: string;
  target_ip: string;
  risk_level: string;
  risk_reasons: string[];
  command_preview: string;
  expires_at: string;
}
```

---

## 10. المكونات المطلوبة للواجهة الحقيقية

### 10.1 مكونات موجودة وجاهزة ✅

| المكون | الحالة | ملاحظات |
|--------|--------|---------|
| `DualPanelLayout` | ✅ جاهز | التخطيط الرئيسي |
| `AIChatPanel` | ✅ جاهز | يحتاج ربط WebSocket |
| `TerminalPanel` | ✅ جاهز | يحتاج ربط WebSocket |
| `Sidebar` | ✅ جاهز | يحتاج ربط التنقل |
| `ApprovalCard` | ✅ جاهز | يحتاج ربط API |
| `AIPlanCard` | ✅ جاهز | - |
| `CredentialCard` | ✅ جاهز | - |
| `SessionCard` | ✅ جاهز | - |
| `VulnerabilityCard` | ✅ جاهز | - |
| `TargetCard` | ✅ جاهز | - |

### 10.2 مكونات تحتاج إنشاء 🔨

| المكون | الأولوية | الوصف |
|--------|----------|-------|
| `MissionList` | عالية | قائمة المهام |
| `MissionCard` | عالية | بطاقة المهمة |
| `MissionDetail` | عالية | تفاصيل المهمة |
| `TargetList` | عالية | قائمة الأهداف |
| `VulnerabilityList` | عالية | قائمة الثغرات |
| `CredentialList` | متوسطة | قائمة بيانات الاعتماد |
| `SessionList` | متوسطة | قائمة الجلسات |
| `KnowledgeBase` | متوسطة | قاعدة المعرفة |
| `TechniqueCard` | متوسطة | بطاقة التقنية |
| `ModuleCard` | متوسطة | بطاقة الوحدة |
| `StatisticsPanel` | متوسطة | لوحة الإحصائيات |
| `RiskMeter` | منخفضة | مقياس المخاطر |
| `TimelineView` | منخفضة | عرض الجدول الزمني |

### 10.3 صفحات تحتاج إنشاء 📄

| الصفحة | الأولوية | الوصف |
|--------|----------|-------|
| `/missions` | عالية | قائمة المهام |
| `/missions/:id` | عالية | تفاصيل المهمة (Operations الحالية) |
| `/knowledge` | متوسطة | قاعدة المعرفة |
| `/knowledge/techniques` | متوسطة | التقنيات |
| `/knowledge/modules` | متوسطة | الوحدات |
| `/settings` | منخفضة | الإعدادات |

---

## 11. خطة التنفيذ المقترحة

### المرحلة 1: البنية التحتية (أسبوع 1)

1. ربط WebSocket الحقيقي مع الخادم
2. إنشاء API client متكامل
3. إضافة state management (Zustand أو Context)
4. إنشاء hooks مخصصة للبيانات

### المرحلة 2: الصفحات الأساسية (أسبوع 2)

1. صفحة قائمة المهام `/missions`
2. تحديث صفحة Operations لاستخدام بيانات حقيقية
3. ربط الطرفية بـ WebSocket

### المرحلة 3: قاعدة المعرفة (أسبوع 3)

1. صفحة Knowledge Base
2. بطاقات التقنيات والوحدات
3. البحث والتصفية

### المرحلة 4: التحسينات (أسبوع 4)

1. الإحصائيات والرسوم البيانية
2. الإشعارات في الوقت الحقيقي
3. تحسين الأداء

---

## 12. ملفات المشروع

```
client/src/
├── components/
│   ├── manus/           # مكونات Manus المخصصة
│   │   ├── AIChatPanel.tsx
│   │   ├── AIPlanCard.tsx
│   │   ├── ApprovalCard.tsx
│   │   ├── ArtifactCard.tsx
│   │   ├── DualPanelLayout.tsx
│   │   ├── EventCard.tsx
│   │   ├── PlanView.tsx
│   │   ├── Sidebar.tsx
│   │   ├── TerminalPanel.tsx
│   │   └── index.ts
│   └── ui/              # مكونات shadcn/ui
├── lib/
│   ├── api.ts           # API client
│   └── utils.ts         # أدوات مساعدة
├── pages/
│   ├── Home.tsx
│   └── Operations.tsx
├── types/
│   └── index.ts         # Type definitions
└── index.css            # Design System CSS
```

---

**تاريخ الإنشاء:** 2 يناير 2026  
**المؤلف:** Manus AI  
**الإصدار:** 1.0
